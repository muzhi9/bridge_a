好的，这里是一份完整的项目方案文档（Markdown格式），供您参考或向团队展示。

------

# 多功能文本处理网关服务 设计方案

## 1. 项目概述

本项目旨在构建一个轻量级、自包含的 **多功能文本处理网关服务**，将多个实用的文本分析、信息挖掘功能整合为统一的 RESTful API，并通过容器化技术实现一键部署。服务面向开发者、内容创作者及数据分析人员，提供便捷的离线文本处理能力，无需依赖任何第三方在线 API，保障数据隐私与稳定性。

## 2. 功能特性

服务规划三个核心功能模块：

### 2.1 文本工具箱

- **拼音转换**：利用 `pypinyin` 将中文文本转换为带声调的拼音。
- **中文分词**：通过 `jieba` 分词库对文本进行词级切分。
- **繁简转换**：基于 `OpenCC` 实现简体中文到繁体中文的转换。
- **字数统计**：自动过滤空白字符，准确统计文本中有效字符数。

### 2.2 网页正文提取

- 输入一个网页 URL，自动抓取页面内容。
- 使用 `readability-lxml` 算法提取文章标题与纯文本正文。
- 去除广告、导航、侧边栏等无关元素，适合用于内容聚合或大模型语料预处理。

### 2.3 离线信息挖掘

- **身份证号码解析**：基于国内行政区划代码（省/市/区三级），解析出生日期、性别及发证地区。
- **手机号码归属地查询**：利用离线手机号段数据库，返回省份、城市及运营商（移动/联通/电信及虚拟运营商）。

> 注：以上两个功能均使用本地离线数据集，不产生网络请求，适用于对数据安全有要求的内部系统。

## 3. 技术选型

| 层级             | 技术             | 理由                                                     |
| :--------------- | :--------------- | :------------------------------------------------------- |
| **编程语言**     | Python 3.11+     | 生态丰富，上述依赖库均为 Python 实现                     |
| **Web 框架**     | FastAPI          | 高性能，自动生成 Swagger 文档，类型安全                  |
| **容器引擎**     | Docker           | 标准化交付，环境隔离，便于编排与扩展                     |
| **离线数据格式** | CSV + 二进制     | CSV 便于人类阅读；`phone.dat` 为业界通用格式，查询效率高 |
| **鉴权方式**     | API Key (Header) | 轻量级，适用于内部或小范围开放场景                       |

## 4. 系统架构

服务采用 **单进程 + 内存缓存** 的架构，所有数据在容器启动时一次性加载至内存，确保毫秒级响应。

```
+-------------------+       +---------------------------------------+
|   外部请求        | ---> |      FastAPI 网关 (main.py)            |
|  (HTTP + API Key) |       |  - 路由分发                            |
+-------------------+       |  - 参数校验与响应模型                   |
                            |  - 异常处理                            |
                            +--------+-------------+----------------+
                                     |             |
                                     v             v
                            +--------+------+  +---+-------------+
                            |  文本处理引擎  |  |  离线数据查询引擎 |
                            | (拼音/分词/   |  |  (身份证/手机号)  |
                            |  繁简/字数)   |  |                   |
                            +---------------+  +-------------------+
                                     ^             ^
                                     |             |
                                     +------+------+
                                            |
                                     +------+------+
                                     | 内存数据缓存 |
                                     | (area_map,  |
                                     | phone_map) |
                                     +-------------+
                                            |
                                     +------+------+
                                     |  离线数据文件 |
                                     | (CSV, .dat) |
                                     +-------------+
```



- **启动流程**：容器启动 → 读取 CSV 和 `phone.dat` → 构建内存字典 → 启动 uvicorn 监听端口。
- **请求处理**：所有请求先经过 API Key 校验，再根据路由调用对应处理器，处理器直接从内存字典取值，无需重复 I/O。

## 5. 接口设计（概要）

| 方法 | 端点                | 功能       | 输入                         | 输出                       |
| :--- | :------------------ | :--------- | :--------------------------- | :------------------------- |
| POST | `/api/tool/text`    | 文本工具箱 | `{ "text": "中文" }`         | 拼音、分词列表、繁体、字数 |
| POST | `/api/tool/extract` | 正文提取   | `{ "url": "..." }`           | 标题、纯文本正文           |
| POST | `/api/tool/idcard`  | 身份证解析 | `{ "id_card": "110101..." }` | 省、市、区、生日、性别     |
| POST | `/api/tool/phone`   | 手机归属地 | `{ "phone": "13800138000" }` | 省份、城市、运营商         |

所有接口均需在请求头中携带 `X-API-Key: <密钥>` 进行鉴权。

服务启动后，可通过 `/docs` 访问交互式 API 文档，直接测试调用。

## 6. 离线数据源说明

为确保信息的准确性与可用性，选用以下开源数据集：

### 身份证地区码

- 来源：[modood/Administrative-divisions-of-China](https://github.com/modood/Administrative-divisions-of-China)
- 数据格式：`provinces.csv`、`cities.csv`、`areas.csv`（UTF-8 编码）
- 更新频率：不定期跟随民政部行政区划调整更新
- 数据完整性：覆盖全国省、市、县三级代码及名称

### 手机号归属地

- 来源：[ls0f/phone](https://github.com/ls0f/phone)（或 [xluohome/phonedata](https://github.com/xluohome/phonedata)）
- 数据格式：`phone.dat`（二进制，包含号段、省份、城市、运营商、邮编、区号等）
- 更新频率：约每年更新一次，支持主流号段
- 数据容量：约 50 万条记录，完全满足国内业务需求

> 注意：上述数据文件需在构建镜像前自行下载并放置于 `data/` 目录。

## 7. 容器化部署方案

### 7.1 镜像构建

编写 `Dockerfile`，基于 `python:3.11-slim` 基础镜像，安装依赖、复制代码及数据文件。

### 7.2 运行环境

- 仅需一个容器，对外暴露一个端口（如 `8000`）。
- 资源需求：约 200 MB 内存（取决于数据文件大小），CPU 无特殊要求。

### 7.3 部署命令

```
# 构建镜像
docker build -t text-gateway .

# 运行容器
docker run -d -p 8080:8000 --name text-gateway text-gateway
```



### 7.4 健康检查

可通过访问根路径 `/` 返回服务状态信息，用于容器编排的健康探针。

## 8. 安全与访问控制

- **API Key 认证**：所有接口均要求提供静态 API Key，防止未授权访问。
- **限流策略**（建议后续增加）：可借助 `slowapi` 或网关层（如 Nginx）实现请求频率限制，避免滥用。
- **数据传输**：生产环境建议配置 HTTPS（使用反向代理如 Nginx 或 Caddy 终止 TLS）。

## 9. 后续扩展方向

- **增加更多文本功能**：如关键词提取、文本摘要、情感分析（可集成轻量级模型）。
- **支持批量处理**：提供批量接口，提升处理效率。
- **用户管理**：支持多用户、动态 Token 生成与管理。
- **日志与监控**：接入 Prometheus 指标，统计各接口调用次数与延迟。
- **多语种支持**：扩展为处理英文、日文等语言（视需求而定）。

## 10. 总结

本方案以“**轻量化、离线化、容器化**”为核心，将常用文本处理工具与信息查询功能打包为统一服务，易于部署、迁移和扩展。既可作为内部基础工具，也适合作为微服务架构中的一环，为其他系统提供稳定的文本处理能力。